import React from "react"
import styled from "styled-components"

const HomePage = () => {
    return (
        <HomePageWrapper>Home Page Content</HomePageWrapper>
    )
}

const HomePageWrapper = styled.div`
    font-size: 72px;
    color: #fff;
    text-align: center;
`

export default HomePage